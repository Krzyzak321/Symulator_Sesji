import org.junit.Test;
import static org.junit.Assert.*;

public class SymulatorSesjiTest {

    @Test
    public void testClassExists() {
        SymulatorSesji s = new SymulatorSesji();
        assertNotNull(s);
    }
}